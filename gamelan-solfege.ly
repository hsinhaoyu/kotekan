% The F-sharp major scale that Colin McPhee used to approximate the Pelog (Selisir mode) scale in Balinese Ceremonial Music 2. Gambangan (1940)
% To use it, (ly:parser-set-note-names pelog-fis)
%    i - ding
%    o - dong
%    e - deng
%    u - dong
%    a - dang
#(define-public pelog-fis
   `((i . ,#{ ais #})		
     (o . ,#{ b #})		
     (e . ,#{ cis #})		
     (u . ,#{ eis #})		
     (a . ,#{ fis #})))		

